<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Authentication{% endblock %} - Cartly</title>

    <!-- Tailwind build -->
    <link rel="stylesheet" href="/assets/css/output.css">

    <!-- Inter font for modern, professional look -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Alpine for small UI interactivity (toasts, etc.) -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    {% block head %}{% endblock %}

    <style>
        [x-cloak]{display:none!important}
    </style>
</head>
<body class="min-h-screen bg-base-200 font-body">
    {% include 'partials/flash_toasts.twig' %}

    <!-- Split layout inspired by Dashwind login -->
    <div class="min-h-screen grid grid-cols-1 md:grid-cols-2">
        <!-- Brand / Promo panel - uses theme primary color gradient -->
        <div class="hidden md:flex relative overflow-hidden bg-neutral text-neutral-content">
            <div class="relative z-10 w-full flex flex-col justify-between p-12 xl:p-16">
                <div>
                    <div class="flex items-center gap-3">
                        <div class="h-10 w-10 rounded-xl bg-neutral-content/10 backdrop-blur-md grid place-items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" class="h-6 w-6">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
                            </svg>
                        </div>
                        <span class="text-xl font-semibold tracking-tight text-neutral-content">Cartly Admin</span>
                    </div>
                    <h1 class="mt-12 text-4xl font-semibold leading-tight tracking-tight text-neutral-content">Manage with clarity.</h1>
                    <p class="mt-4 text-base leading-relaxed max-w-md text-neutral-content/80">A focused, professional dashboard experience for managing shops, packages, and users—all in one place.</p>
                </div>
                <div class="text-sm text-neutral-content/70">
                    <p>© {{ "now"|date("Y") }} Cartly. All rights reserved.</p>
                </div>
            </div>
        </div>

        <!-- Auth form panel -->
        <div class="flex items-center justify-center p-6 sm:p-8">
            <div class="w-full max-w-md">
                {% block content %}{% endblock %}
            </div>
        </div>
    </div>

    {% block scripts %}{% endblock %}
</body>
</html>
