<section class="rounded-lg bg-base-200 shadow-sm p-8 md:p-12 text-center">
    <p class="text-sm uppercase tracking-wide text-primary font-semibold">
        {{ hero_settings.kicker ?? 'Minimal and fast' }}
    </p>
    <h1 class="mt-3 text-4xl md:text-6xl font-bold">
        {{ hero_settings.headline ?? ('Shop smarter with ' ~ shop.shop_name) }}
    </h1>
    <p class="mt-4 text-lg text-base-content/70 max-w-2xl mx-auto">
        {{ hero_settings.subheadline ?? 'Launch your storefront in minutes with trusted payments, fast delivery, and seamless support.' }}
    </p>
    <div class="mt-6 flex flex-wrap justify-center gap-3">
        <a href="{{ hero_settings.cta_url ?? '/products' }}" class="btn btn-primary">
            {{ hero_settings.cta_label ?? 'Start Shopping' }}
        </a>
        <a href="/categories" class="btn btn-outline btn-primary">Browse Collections</a>
    </div>
</section>
