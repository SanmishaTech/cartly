<!DOCTYPE html>
<html lang="en" data-theme="coral">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ shop.shop_name }}{% endblock %}</title>
    
    {# Compiled Tailwind + DaisyUI CSS #}
    <link rel="stylesheet" href="/assets/css/output.css">
    
    {# Alpine.js for interactivity #}
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    
    {# Theme-specific styles #}
    <link rel="stylesheet" href="{{ theme_asset('css/style.css') }}">
    
    {% if third_party and third_party.head_html %}
        {{ third_party.head_html|raw }}
    {% endif %}

    {% block head %}{% endblock %}
</head>
<body class="bg-base-100 text-base-content">
    {% if third_party and third_party.body_start_html %}
        {{ third_party.body_start_html|raw }}
    {% endif %}
    {# Header/Navbar #}
    {% include 'partials/header.twig' %}
    
    {# Main Content #}
    <main>
        {% block content %}{% endblock %}
    </main>
    
    {# Footer #}
    {% include 'partials/footer.twig' %}
    
    {# Theme-specific scripts #}
    <script src="{{ theme_asset('js/script.js') }}"></script>
    
    {% block scripts %}{% endblock %}
    
    {% if third_party and third_party.body_end_html %}
        {{ third_party.body_end_html|raw }}
    {% endif %}
</body>
</html>
